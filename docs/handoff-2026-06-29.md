# Session Handoff — 2026-06-29 (current state / continue from here)

> **Authoritative** doc of the current state. History: [handoff-2026-06-28](handoff-2026-06-28.md) (the 9 phases),
> [handoff-2026-06-26](handoff-2026-06-26.md) (planning), [blueprint](blueprint-schema-canonico.md) (spec).
> In a new session, read this first.

## ▶️ Start here (office)

1. **Clone/update:** `git pull` in the umbrella + `git submodule update --init --recursive`
   (or `git submodule update --remote --merge`). Everything is on `origin` — nothing local pending.
2. **Sanity:** `cd BueloApi && dotnet test` (should give **185/185**). Frontend: `pnpm install && pnpm build`.
3. **Ask it to read this handoff** and pick the first pending item (I recommend this order):
   **(a)** generate the **EF migrations** resolving the Roslyn conflict (see §Pending #1) — unblocks prod;
   **(b)** unblock **vite** (swap the `vite-plugin-monaco-editor`); **(c)** see the **YAML autocomplete**
   in the browser (`dotnet run` + `pnpm dev` + create `algo.report.yml`).

## ✅ State: declarative engine complete, committed, and on remote

**Backend (`BueloApi`): `dotnet build` + 185 tests green.** Frontend (`BueloWeb`): typecheck + `pnpm build` green.
Everything **committed and pushed** (origin) — clean working tree in all 3 repos:

| Repo | Commit on origin/master |
|---|---|
| BueloApi | `abf7274` (declarative engine + Excel/validate/html/migration-ready) |
| BueloWeb | `2d4475b` (Monaco YAML + monaco-yaml + package updates) |
| umbrella | `d049092` (pointer bumps + this handoff) |

### What exists (recap)
- **Declarative engine** YAML→IR(`BueloDocument`)→QuestPDF, parallel to C#/Roslyn. Parser, expression
  engine (stdlib + aggregation), blocks (text/markdown/**html**/table/row/column/card/image/spacer/
  divider/pageBreak), bands, modules (styles/component/theme/formats/lib/validator).
- **Recipes:** PDF (QuestPDF) **and Excel** (ClosedXML) from the same IR — `?format=excel`.
- **Validators** 3 steps + **`validate:` at render** (fail-fast gate → 400) + `POST api/validate-data`.
- **Persistence:** `IDefinitionStore` (fs/InMemory) for definitions; operational EF Core (SQLite/Postgres)
  for render history. **Migration-ready** startup (`Migrate()` if migrations exist, otherwise `EnsureCreated()`).
- **Eject** C# (round-trip validated). **JSON Schema** per kind (`api/schemas`) + **Monaco** (monaco-yaml).
- **Hardening:** Roslyn assembly cache, opt-in Bearer token + `/ping`, **render-timeout** (503).
- **Examples:** `Buelo.Api/definitions/` (hello/invoice/colaboradores + modules + mock data).
- **Verified live** (06/28 session): all endpoints + EF logging to SQLite + frontend serving.

### New endpoints
`render-declarative` (`?format=pdf|excel`), `render-stored/{name}` (`?format`), `eject`, `GET schemas[/{kind}]`,
`validate-data`, `render-history`, `/ping`.

## ⏳ Pending (continue from here)

1. **EF migrations — blocked by a dependency conflict.** The app is already migration-ready, but generating the
   files with `dotnet ef migrations add` fails: **EF Design 10.0.9 → Roslyn (CodeAnalysis) 5.0.0**, which
   conflicts with the **5.3.0** that `Buelo.Engine` uses (NU1107). Options: (a) isolated tooling project for
   migrations; (b) align the Roslyn versions; (c) proceed with `EnsureCreated` (works for self-host). The
   `dotnet-ef 10.0.9` is already installed globally. Target command:
   `dotnet ef migrations add InitialCreate --project Buelo.Engine --startup-project Buelo.Api -o Persistence/Migrations`.
2. **Frontend — `vite` stuck on major 6.** vite 7/8 break the patched `vite-plugin-monaco-editor@1.1.0`
   (`MODULE_NOT_FOUND` in workerMiddleware). Swap/update that plugin before bumping vite.
3. **Frontend — `lucide-vue-next` deprecated** → migrate imports to `@lucide/vue` (mechanical refactor across several
   components).
4. **Live verification of YAML autocomplete in the browser** — the bundle has the yaml worker and the schemas, but
   you can only confirm autocomplete/validation by opening the editor (`pnpm dev` + create `algo.report.yml`).
5. Minor v1 limitations: width `%` ≈ relative weight; image only base64/local file; the theme palette doesn't
   resolve color names; eject degrades image to a placeholder; Excel flattens layout (table becomes a worksheet).

## ▶️ How to run
- API: `cd BueloApi && dotnet run --project Buelo.Api` (http://localhost:5238; `/ping`, `/api/schemas`).
- Frontend: `pnpm` is at `C:\Users\jhona\AppData\Local\pnpm\bin` (set `$env:PNPM_HOME` + PATH);
  `pnpm install` (allowBuilds already resolved) and `pnpm dev` (http://localhost:5173).
- Example render: `POST api/report/render-stored/invoice` with body `{ "data": <definitions/data/invoice.json> }`
  (git-bash curl on Windows corrupts accents — send the JSON via a UTF-8 file `--data-binary @`).
